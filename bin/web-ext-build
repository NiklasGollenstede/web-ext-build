#!/usr/bin/env node

module.exports = require('..')(require('json5').parse(process.argv[2] || '{ }'))
.then(name => console.log('Build done:', name))
.catch(error => {
	console.error(error);
	process.exit(1);
});
